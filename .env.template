# Admin App Configuration
ADMIN_APP_BASE_URL=http://localhost:3000  # Base URL for admin app
ADMIN_API_SECRET_KEY=your-secure-secret-key  # JWT signing secret (same as in admin app's env variables)

# LLM Configuration
LLM_SERVER=http://localhost:8080  # Local LLM server endpoint

# WebSocket Server Configuration
WEBSOCKET_SERVER_URL=ws://localhost:5001  # WebSocket server URL for STT/TTS services (from stt-tts-compiled-whisper-server)

# Application Server Configuration
APP_PORT=8009  # Port for the main application server 